import { FC } from 'react';

import { I{{ pascalCase name }}Props } from './{{ pascalCase name }}.props'

const {{ pascalCase name }}: FC<I{{ pascalCase name }}Props> = ({ children }): JSX.Element => {
  return <span>{children}</span>;
};

export default {{ pascalCase name }}
